    !!! Important: before get this repository you need to install node.js + npm + gulp(globally) - this all for proper work of the modules what insight the project. Because insight there installed locally some modules/plugins, that will be used buy gulp, that need to be installed for such purpose.

    !!! List of some gulp tasks:
watch
     - In particular automatically renew css thru scss

serv-watch
    - Start a server and watch for the changes in assets to reload server (also renews css)

build
    - One task for building project to distribution (mus be changed if added file mainly to the production folder, but not to sub folders)

clean
    - One task for cleaning after job
